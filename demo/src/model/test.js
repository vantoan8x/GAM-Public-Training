const TestModel = {
    priKeys: [
    ],

    allKeys: [],

    validator: {
        _id    : `A.isNumber`,
        infor  : `A.isObject`
    },
};

TestModel.subKey = {
}

module.exports = TestModel;
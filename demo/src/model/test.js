const TestModel = {
    priKeys: [
    ],

    allKeys: [],

    validator: {
        user    : `A.isString`,
        name  : `A.isString`
    },
};

TestModel.subKey = {
}

module.exports = TestModel;
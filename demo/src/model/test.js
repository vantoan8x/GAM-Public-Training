const TestModel = {
    priKeys: [
    ],

    allKeys: [],

    validator: {
        name    : `A.isString`,
        age     : `A.isNumber`,
        address : `A.isString`
    },
};

TestModel.subKey = {
}

module.exports = TestModel;